const arr = Array(1001).fill().map(() => Array(1001).fill(0));

function solution(line) {
    line.forEach(v => {
        const y = v[0], x = v[1], t = v[2];
        for(let i = 0; ; i++){
            let yy, xx;
            if(y === 0) {
                
            } else {

            }
            if(x === 0) {

            } else {

            }
        }
        for(let i = 0; ; i--){
            
        }
    })
}